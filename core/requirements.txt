# Taxi Driver - Reinforcement Learning Project
# Python 3.11.x (testé avec 3.11.9)

gymnasium[toy-text]>=0.29.0
numpy>=1.24.0
matplotlib>=3.7.0
# Deep Q-Network agent (--agent D). CPU build is enough for Taxi:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
